{
  "name": "bundle-size",
  "scripts": {
    "all": "npm run generate && npm run build && npm run test",
    "generate": "npm run --workspaces generate",
    "build": "npm run --workspaces build",
    "test": "npm run --workspaces test",
    "size": "node README.mjs",
    "ci": "npm run generate && npm run build && npm run test && npm run size"
  },
  "workspaces": [
    "./esbuild",
    "./parcel",
    "./rollup",
    "./vite",
    "./webpack"
  ],
  "//": "parcel adds the dep on process",
  "@parcel/resolver-default": {
    "packageExports": true
  },
  "devDependencies": {
    "process": "^0.11.10",
    "prettier": "^3.2.5"
  }
}
